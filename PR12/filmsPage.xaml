<Page x:Class="PR12.filmsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:PR12"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="filmsPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">

            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="Фильмы" FontSize="20" VerticalAlignment="Center" HorizontalAlignment="Center"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Grid.Column="1" x:Name="toregistrBtn" Content="Войти в акк" FontSize="16" Width="100" Height="40" Margin="0,0,20,0" VerticalAlignment="Center" Click="goToReg_Click"/>
                <Button Grid.Column="1" x:Name="toakkBtn" Content="Аккаунт" FontSize="16" Width="100" Height="40" Margin="0,0,20,0" VerticalAlignment="Center" Click="goToAkk_Click"/>
            </StackPanel>
        </Grid>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" PanningMode="VerticalOnly" CanContentScroll="True">

            <ListBox x:Name="filmsList" BorderThickness="0" ScrollViewer.HorizontalScrollBarVisibility="Disabled" ScrollViewer.VerticalScrollBarVisibility="Disabled" HorizontalContentAlignment="Stretch">
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel IsItemsHost="True" HorizontalAlignment="Left"/>
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>

                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Width="250" Height="435" Margin="10" Padding="10" BorderBrush="Gray" BorderThickness="1" CornerRadius="8">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <Image Grid.Row="0" Source="{Binding ImagePath}" Width="180" Height="220" Stretch="UniformToFill" Margin="0,0,0,5"/>

                                <StackPanel Grid.Row="1">
                                    <TextBlock Text="{Binding FilmName}" FontSize="16" FontWeight="Bold" TextWrapping="Wrap"/>
                                    <TextBlock Text="{Binding Description}" FontSize="14" TextWrapping="Wrap" Margin="0,5,0,0"/>
                                    <TextBlock Text="{Binding FilmRating}" FontSize="15" Margin="0,5,0,0"/>
                                    <TextBlock Text="{Binding StartDate}" FontSize="14" Margin="0,5,0,0"/>
                                    <TextBlock Text="{Binding Description}" FontSize="14" TextWrapping="Wrap" Margin="0,5,0,0"/>
                                </StackPanel>

                                <Button Grid.Row="2" x:Name="buyBtn" Content="Купить билеты" Width="100" Height="30" Margin="0,10,0,15" HorizontalAlignment="Center" Click="buyBtn_Click"/>
                            </Grid>

                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </ScrollViewer>
    </Grid>
</Page>
